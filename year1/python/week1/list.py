# Create a list with elements of different types
list = [1, 2, 3, "Hello", [1, "world"]]

# Loop through the elements of the list printing each element and its type

for item in list:
    print(item, " is of type ", type(item))
